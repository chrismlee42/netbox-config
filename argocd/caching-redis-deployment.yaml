apiVersion: apps/v1
kind: Deployment
metadata:
  name: caching-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: caching-redis
  template:
    metadata:
      labels:
        app: caching-redis
    spec:
      containers:
      - name: redis
        image: redis:latest
        ports:
        - containerPort: 6379
