apiVersion: apps/v1
kind: Deployment
metadata:
  name: caching-redis
spec:
  replicas: 1
  selector:
    app.kubernetes.io/component: caching-redis
  template:
    metadata:
      labels:
        app.kubernetes.io/component: caching-redis
    spec:
      containers:
      - name: redis
        image: redis:latest
        ports:
        - containerPort: 6379
